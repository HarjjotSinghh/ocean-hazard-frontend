'use client'

// Better Auth doesn't require a provider component
// This is just a placeholder for future auth context if needed
export function BetterAuthProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}